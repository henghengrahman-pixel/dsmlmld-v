const $=s=>document.querySelector(s); let currentChat=null;
async function api(url,opt={}){const r=await fetch(url,{...opt,headers:{'Content-Type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
function esc(s=''){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function setState(el,ok,label){el.textContent=label;el.className=ok?'ok':'bad'}
async function boot(){const me=await api('/api/me');if(me.ok){$('#app').classList.remove('hidden');await loadStatus()}else{$('#login').classList.remove('hidden')}}
$('#loginBtn').onclick=async()=>{try{await api('/api/login',{method:'POST',body:JSON.stringify({username:$('#u').value,password:$('#p').value})});location.reload()}catch(e){$('#loginErr').textContent=e.message}};
$('#logoutBtn').onclick=async()=>{await api('/api/logout',{method:'POST'});location.reload()};
document.querySelectorAll('nav button[data-tab]').forEach(b=>b.onclick=async()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));$(`#tab-${b.dataset.tab}`).classList.add('active');if(b.dataset.tab==='chats')loadChats();if(b.dataset.tab==='rules')loadRules();if(b.dataset.tab==='knowledge')loadKb();if(b.dataset.tab==='logs')loadLogs()});
async function loadStatus(){const s=await api('/api/status');setState($('#sDb'),s.db,s.db?'CONNECTED':'ERROR');setState($('#sLc'),s.livechatConfigured,s.livechatConfigured?'CONFIGURED':'MISSING');setState($('#sAi'),s.openaiConfigured,s.openaiConfigured?`READY · ${s.model}`:'MISSING');setState($('#sPoll'),s.poller?.running||s.poller?.mode==='polling',`${s.poller?.mode||'-'} · ${s.poller?.pollMs||0}ms`);$('#autoReply').checked=s.autoReply;$('#warnings').textContent=(s.warnings||[]).join(' • ')||'Tidak ada warning konfigurasi.'}
$('#autoReply').onchange=async e=>{if(e.target.checked&&!confirm('Aktifkan Auto Reply? Pastikan Test LiveChat dan OpenAI sudah berhasil.')){e.target.checked=false;return}await api('/api/settings/auto-reply',{method:'POST',body:JSON.stringify({enabled:e.target.checked})});await loadStatus()};
function testBtn(id,url){$(id).onclick=async()=>{const out=$('#testOut');out.textContent='Testing...';try{out.textContent=JSON.stringify(await api(url,{method:'POST'}),null,2)}catch(e){out.textContent='ERROR: '+e.message}}}
testBtn('#testLc','/api/test/livechat');testBtn('#testAi','/api/test/openai');testBtn('#testSync','/api/test/sync');
$('#testTypo').onclick=async()=>{$('#typoOut').textContent=JSON.stringify(await api('/api/test/typo',{method:'POST',body:JSON.stringify({text:$('#typoText').value})}),null,2)};
async function loadChats(){const d=await api('/api/conversations');$('#chatList').innerHTML=d.items.map(x=>`<div class="chatitem" data-id="${esc(x.chat_id)}"><b>${esc(x.customer_name||x.chat_id)}</b><small>${esc(x.customer_email||'')} · ${x.human_takeover_until?'HUMAN TAKEOVER':'AI READY'}</small></div>`).join('')||'<div class="chatitem">Belum ada chat.</div>';document.querySelectorAll('.chatitem[data-id]').forEach(el=>el.onclick=()=>openChat(el.dataset.id,el.textContent.trim()))}
async function openChat(id,title){currentChat=id;$('#chatTitle').textContent=title;const d=await api('/api/conversations/'+encodeURIComponent(id));$('#messages').innerHTML=d.items.map(m=>`<div class="msg ${esc(m.sender_type)}"><div class="who">${esc(m.sender_type)}</div>${esc(m.text)}</div>`).join('');$('#messages').scrollTop=$('#messages').scrollHeight}
$('#refreshChats').onclick=loadChats;$('#takeoverBtn').onclick=async()=>{if(!currentChat)return;await api(`/api/conversations/${encodeURIComponent(currentChat)}/takeover`,{method:'POST'});loadChats()};$('#enableAiBtn').onclick=async()=>{if(!currentChat)return;await api(`/api/conversations/${encodeURIComponent(currentChat)}/enable-ai`,{method:'POST'});loadChats()};$('#sendManual').onclick=async()=>{if(!currentChat||!$('#manualText').value.trim())return;await api(`/api/conversations/${encodeURIComponent(currentChat)}/send`,{method:'POST',body:JSON.stringify({text:$('#manualText').value})});$('#manualText').value='';setTimeout(()=>openChat(currentChat,$('#chatTitle').textContent),1000)};
async function loadRules(){const d=await api('/api/rules');$('#rulesList').innerHTML=d.items.map(x=>`<div class="tr"><span class="pill">${esc(x.category)}</span><b>${esc(x.rule_type)}</b><span>${esc(x.content)}</span><button data-delrule="${x.id}">Hapus</button></div>`).join('')||'<div class="tr">Belum ada rule.</div>';document.querySelectorAll('[data-delrule]').forEach(b=>b.onclick=async()=>{await api('/api/rules/'+b.dataset.delrule,{method:'DELETE'});loadRules()})}
$('#addRule').onclick=async()=>{await api('/api/rules',{method:'POST',body:JSON.stringify({category:$('#ruleCategory').value,ruleType:$('#ruleType').value,content:$('#ruleContent').value})});$('#ruleContent').value='';loadRules()};
async function loadKb(){const d=await api('/api/knowledge');$('#kbList').innerHTML=d.items.map(x=>`<div class="tr"><span class="pill">${esc(x.category)}</span><b>${esc(x.title)}</b><span>${esc(x.content)}</span><button data-delkb="${x.id}">Hapus</button></div>`).join('')||'<div class="tr">Belum ada knowledge.</div>';document.querySelectorAll('[data-delkb]').forEach(b=>b.onclick=async()=>{await api('/api/knowledge/'+b.dataset.delkb,{method:'DELETE'});loadKb()})}
$('#addKb').onclick=async()=>{await api('/api/knowledge',{method:'POST',body:JSON.stringify({category:$('#kbCategory').value,title:$('#kbTitle').value,content:$('#kbContent').value})});$('#kbTitle').value='';$('#kbContent').value='';loadKb()};
async function loadLogs(){const [a,e]=await Promise.all([api('/api/logs/ai'),api('/api/logs/errors')]);$('#aiLogs').innerHTML=a.items.map(x=>`<div class="tr"><span>${esc(x.intent||'-')}</span><span>${esc(x.action||'-')} ${(x.confidence??'')}</span><span>${esc(x.reply||x.error||'-')}</span><span>${new Date(x.created_at).toLocaleString()}</span></div>`).join('')||'Belum ada AI log.';$('#errLogs').innerHTML=e.items.map(x=>`<div class="tr"><span>${esc(x.source)}</span><span>${esc(x.code||'-')}</span><span>${esc(x.message)}</span><span>${new Date(x.created_at).toLocaleString()}</span></div>`).join('')||'Belum ada error.'}
$('#refreshLogs').onclick=loadLogs;
boot().catch(e=>{document.body.innerHTML='<pre style="color:#fff">BOOT UI ERROR: '+esc(e.message)+'</pre>'});
